# Instructions for AI agents working on zterm

- Test compile with 'make BEAR= debug', if a sandbox is supported, this should
  be run inside the sandbox.

- Do not attempt to keep legacy compatibility with code that has not yet been committed.

- Remember: DRY (Don't Repeat Yourself), adding generic helpers is preferred over
  reimplementing the same logic more than once.
